type Product = {
	id: string;
	name: string;
	description: string;
	price: string;
	images?: string[];
	image?: string;
	brand: string;
	model: string;
	categoryId: string;
	oldPrice?: string;
	newPrice?: string;
};
